# Lasso Regression Notebooks

Recommended notebook ideas:

- `lasso_feature_selection.ipynb` — Visualise sparsity patterns as `alpha` increases.
- `lasso_vs_ridge.ipynb` — Compare validation scores and selected features across penalties.
