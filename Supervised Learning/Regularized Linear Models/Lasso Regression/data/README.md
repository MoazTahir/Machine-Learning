# Lasso Regression Dataset

Synthetic regression data containing redundant features meant to be pruned by the L1 penalty. Replace `lasso_regression.csv` with your own dataset to experiment on real projects.
